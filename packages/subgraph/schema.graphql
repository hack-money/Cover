type OptionCreation @entity {
  id: ID!
  holder: Bytes!
  fee: BigInt!
  premium: BigInt!
}

type OptionExercise @entity {
    id: ID!
    exchangeAmout: BigInt!
}

type OptionExpiry @entity {
    id: ID!
}
